# v25.0 compatibility pack

Version-sensitive simulator data stays separate from the domain engine. `errors.json` is the initial implemented subset. Confidence labels are `documented`, `observed` (authorized test account), and `approximated` (Meta does not publish an exact contract).

